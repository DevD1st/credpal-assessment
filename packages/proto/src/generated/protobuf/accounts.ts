// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v5.29.3
// source: protobuf/accounts.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Error } from "./common.js";

export const protobufPackage = "accounts";

/**
 * ------------------------------------------
 * Auths
 * ------------------------------------------
 */
export interface AuthCredentials {
  accessToken: AuthToken | undefined;
  refreshToken: AuthToken | undefined;
}

export interface AuthToken {
  token: string;
  expiresIn: string;
}

/**
 * ------------------------------------------
 * Requests
 * ------------------------------------------
 */
export interface RegisterAccountInput {
  email: string;
  password: string;
}

/**
 * ------------------------------------------
 * Responses
 * ------------------------------------------
 */
export interface OTPExpiration {
  expAt: string;
}

export interface OTPExpirationResponse {
  data?: OTPExpiration | undefined;
  error?: Error | undefined;
}

function createBaseAuthCredentials(): AuthCredentials {
  return { accessToken: undefined, refreshToken: undefined };
}

export const AuthCredentials: MessageFns<AuthCredentials> = {
  encode(message: AuthCredentials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accessToken !== undefined) {
      AuthToken.encode(message.accessToken, writer.uint32(10).fork()).join();
    }
    if (message.refreshToken !== undefined) {
      AuthToken.encode(message.refreshToken, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthCredentials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthCredentials();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accessToken = AuthToken.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.refreshToken = AuthToken.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthCredentials {
    return {
      accessToken: isSet(object.accessToken)
        ? AuthToken.fromJSON(object.accessToken)
        : isSet(object.access_token)
        ? AuthToken.fromJSON(object.access_token)
        : undefined,
      refreshToken: isSet(object.refreshToken)
        ? AuthToken.fromJSON(object.refreshToken)
        : isSet(object.refresh_token)
        ? AuthToken.fromJSON(object.refresh_token)
        : undefined,
    };
  },

  toJSON(message: AuthCredentials): unknown {
    const obj: any = {};
    if (message.accessToken !== undefined) {
      obj.accessToken = AuthToken.toJSON(message.accessToken);
    }
    if (message.refreshToken !== undefined) {
      obj.refreshToken = AuthToken.toJSON(message.refreshToken);
    }
    return obj;
  },

  create(base?: DeepPartial<AuthCredentials>): AuthCredentials {
    return AuthCredentials.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthCredentials>): AuthCredentials {
    const message = createBaseAuthCredentials();
    message.accessToken = (object.accessToken !== undefined && object.accessToken !== null)
      ? AuthToken.fromPartial(object.accessToken)
      : undefined;
    message.refreshToken = (object.refreshToken !== undefined && object.refreshToken !== null)
      ? AuthToken.fromPartial(object.refreshToken)
      : undefined;
    return message;
  },
};

function createBaseAuthToken(): AuthToken {
  return { token: "", expiresIn: "" };
}

export const AuthToken: MessageFns<AuthToken> = {
  encode(message: AuthToken, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    if (message.expiresIn !== "") {
      writer.uint32(18).string(message.expiresIn);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthToken {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthToken();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expiresIn = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthToken {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      expiresIn: isSet(object.expiresIn)
        ? globalThis.String(object.expiresIn)
        : isSet(object.expires_in)
        ? globalThis.String(object.expires_in)
        : "",
    };
  },

  toJSON(message: AuthToken): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.expiresIn !== "") {
      obj.expiresIn = message.expiresIn;
    }
    return obj;
  },

  create(base?: DeepPartial<AuthToken>): AuthToken {
    return AuthToken.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthToken>): AuthToken {
    const message = createBaseAuthToken();
    message.token = object.token ?? "";
    message.expiresIn = object.expiresIn ?? "";
    return message;
  },
};

function createBaseRegisterAccountInput(): RegisterAccountInput {
  return { email: "", password: "" };
}

export const RegisterAccountInput: MessageFns<RegisterAccountInput> = {
  encode(message: RegisterAccountInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterAccountInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterAccountInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterAccountInput {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
    };
  },

  toJSON(message: RegisterAccountInput): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterAccountInput>): RegisterAccountInput {
    return RegisterAccountInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterAccountInput>): RegisterAccountInput {
    const message = createBaseRegisterAccountInput();
    message.email = object.email ?? "";
    message.password = object.password ?? "";
    return message;
  },
};

function createBaseOTPExpiration(): OTPExpiration {
  return { expAt: "" };
}

export const OTPExpiration: MessageFns<OTPExpiration> = {
  encode(message: OTPExpiration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.expAt !== "") {
      writer.uint32(10).string(message.expAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OTPExpiration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOTPExpiration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.expAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OTPExpiration {
    return {
      expAt: isSet(object.expAt)
        ? globalThis.String(object.expAt)
        : isSet(object.exp_at)
        ? globalThis.String(object.exp_at)
        : "",
    };
  },

  toJSON(message: OTPExpiration): unknown {
    const obj: any = {};
    if (message.expAt !== "") {
      obj.expAt = message.expAt;
    }
    return obj;
  },

  create(base?: DeepPartial<OTPExpiration>): OTPExpiration {
    return OTPExpiration.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OTPExpiration>): OTPExpiration {
    const message = createBaseOTPExpiration();
    message.expAt = object.expAt ?? "";
    return message;
  },
};

function createBaseOTPExpirationResponse(): OTPExpirationResponse {
  return { data: undefined, error: undefined };
}

export const OTPExpirationResponse: MessageFns<OTPExpirationResponse> = {
  encode(message: OTPExpirationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      OTPExpiration.encode(message.data, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      Error.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OTPExpirationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOTPExpirationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = OTPExpiration.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = Error.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OTPExpirationResponse {
    return {
      data: isSet(object.data) ? OTPExpiration.fromJSON(object.data) : undefined,
      error: isSet(object.error) ? Error.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: OTPExpirationResponse): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = OTPExpiration.toJSON(message.data);
    }
    if (message.error !== undefined) {
      obj.error = Error.toJSON(message.error);
    }
    return obj;
  },

  create(base?: DeepPartial<OTPExpirationResponse>): OTPExpirationResponse {
    return OTPExpirationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OTPExpirationResponse>): OTPExpirationResponse {
    const message = createBaseOTPExpirationResponse();
    message.data = (object.data !== undefined && object.data !== null)
      ? OTPExpiration.fromPartial(object.data)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null) ? Error.fromPartial(object.error) : undefined;
    return message;
  },
};

export type AuthServiceDefinition = typeof AuthServiceDefinition;
export const AuthServiceDefinition = {
  name: "AuthService",
  fullName: "accounts.AuthService",
  methods: {
    registerAccount: {
      name: "RegisterAccount",
      requestType: RegisterAccountInput as typeof RegisterAccountInput,
      requestStream: false,
      responseType: OTPExpirationResponse as typeof OTPExpirationResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
